ZMixpanel
===========

ZMixpanel provides support to call the Mixpanel analytics platform
to send people and event data from C# server code. It also includes
MVC Helper classes that emit JavaScript to call Mixpanel from the 
web client.